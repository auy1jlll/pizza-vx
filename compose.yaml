services:
  typescript-app:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: typescript-app
    restart: unless-stopped
    init: true
    # env_file: ./.env  # Uncomment if .env file exists
    ports:
      - "3000:3000"  # Next.js default port
    # If you add a database or other services, add depends_on here
    # networks: [appnet]  # Uncomment if you add more services/networks

# No external services (databases, cache, etc.) were detected in the provided context.
# If your app requires a database (e.g., PostgreSQL, MySQL), add it below and configure networking and volumes as needed.

# networks:
#   appnet:
#     driver: bridge
